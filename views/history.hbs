<!DOCTYPE html>
<html lang="{{lang}}">
{{> dashboard-header}}
<style>
    :root {
        /* Primary Colors */
        --primary: #8240FF;
        --primary-light: #D2B8FF;
        --primary-gradient: linear-gradient(135deg, #D2B8FF 0%, #8240FF 100%);

        /* Background Colors */
        --bg-primary: #0f0f1a;
        --bg-card: rgba(255, 255, 255, 0.04);
        --bg-card-hover: rgba(255, 255, 255, 0.08);
        --bg-input: rgba(255, 255, 255, 0.06);

        /* Border Colors */
        --border: rgba(255, 255, 255, 0.08);
        --border-hover: rgba(130, 64, 255, 0.4);

        /* Text Colors */
        --text-primary: #ffffff;
        --text-secondary: rgba(255, 255, 255, 0.7);
        --text-muted: rgba(255, 255, 255, 0.5);

        /* Semantic Colors */
        --success: #10b981;
        --success-light: #34d399;
        --warning: #f59e0b;
        --warning-light: #fbbf24;
        --danger: #ef4444;
        --danger-light: #f87171;
        --info: #3b82f6;
        --info-light: #60a5fa;
    }

    * {
        box-sizing: border-box;
    }

    body {
        background: var(--bg-primary);
        min-height: 100vh;
        color: var(--text-primary);
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
    }

    .history-container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 80px 16px 100px;
    }

    /* Page Header */
    .history-header {
        margin-bottom: 32px;
    }

    .history-title {
        font-size: 1.75rem;
        font-weight: 700;
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 12px;
        color: var(--text-primary);
    }

    .history-title i {
        color: var(--primary-light);
    }

    .history-subtitle {
        color: var(--text-secondary);
        font-size: 0.95rem;
    }

    /* Filter Card */
    .filter-card {
        background: var(--bg-card);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 1.25rem;
        margin-bottom: 1.5rem;
    }

    .filter-section {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
        align-items: center;
    }

    .filter-btn {
        background: var(--bg-input);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 12px;
        padding: 0.625rem 1.25rem;
        color: var(--text-secondary);
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s ease;
        display: inline-flex;
        align-items: center;
        gap: 8px;
    }

    .filter-btn:hover {
        background: rgba(255, 255, 255, 0.1);
        border-color: var(--primary);
        color: var(--primary-light);
    }

    .filter-btn.active {
        background: var(--primary-gradient);
        border: none;
        color: #fff;
        box-shadow: 0 4px 12px rgba(130, 64, 255, 0.3);
    }

    .filter-btn.active:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(130, 64, 255, 0.4);
    }

    /* Character Filter Carousel */
    .character-filter-section {
        background: var(--bg-card);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 1.25rem;
        margin-bottom: 1.5rem;
    }

    .character-filter-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 16px;
    }

    .character-filter-title {
        font-size: 0.85rem;
        font-weight: 600;
        color: var(--text-secondary);
        text-transform: uppercase;
        letter-spacing: 0.5px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .character-filter-title i {
        color: var(--primary-light);
    }

    .character-nav-buttons {
        display: flex;
        gap: 8px;
    }

    .character-nav-btn {
        width: 36px;
        height: 36px;
        border-radius: 10px;
        background: var(--bg-input);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: var(--text-secondary);
        cursor: pointer;
        transition: all 0.2s ease;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .character-nav-btn:hover:not(:disabled) {
        background: rgba(130, 64, 255, 0.2);
        border-color: var(--primary);
        color: var(--primary-light);
    }

    .character-nav-btn:disabled {
        opacity: 0.3;
        cursor: not-allowed;
    }

    .character-carousel-wrapper {
        position: relative;
        overflow: hidden;
    }

    .character-carousel {
        display: flex;
        gap: 12px;
        overflow-x: auto;
        scroll-behavior: smooth;
        scrollbar-width: none;
        -ms-overflow-style: none;
        padding: 4px 0;
    }

    .character-carousel::-webkit-scrollbar {
        display: none;
    }

    .character-filter-item {
        flex-shrink: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 8px;
        cursor: pointer;
        padding: 12px;
        border-radius: 16px;
        background: var(--bg-input);
        border: 2px solid transparent;
        transition: all 0.3s ease;
        min-width: 100px;
        max-width: 100px;
    }

    .character-filter-item:hover {
        background: var(--bg-card-hover);
        border-color: var(--border-hover);
        transform: translateY(-4px);
    }

    .character-filter-item.active {
        background: rgba(130, 64, 255, 0.15);
        border-color: rgba(130, 64, 255, 0.6);
        box-shadow: 0 8px 24px rgba(130, 64, 255, 0.25);
    }

    .character-filter-avatar {
        position: relative;
        width: 64px;
        height: 64px;
    }

    .character-filter-avatar img {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid rgba(255, 255, 255, 0.1);
        transition: all 0.3s ease;
    }

    .character-filter-item.active .character-filter-avatar img {
        border-color: rgba(130, 64, 255, 0.8);
        box-shadow: 0 0 20px rgba(130, 64, 255, 0.4);
    }

    .character-filter-avatar .all-icon {
        width: 100%;
        height: 100%;
        border-radius: 50%;
        background: var(--primary-gradient);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        color: white;
    }

    .character-filter-count {
        position: absolute;
        bottom: -4px;
        right: -4px;
        background: var(--primary-gradient);
        color: white;
        font-size: 0.7rem;
        font-weight: 600;
        padding: 2px 8px;
        border-radius: 12px;
        min-width: 24px;
        text-align: center;
    }

    .character-filter-name {
        font-size: 0.8rem;
        font-weight: 500;
        color: var(--text-primary);
        text-align: center;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        width: 100%;
    }

    .character-filter-item.active .character-filter-name {
        color: var(--primary-light);
    }

    /* Character Groups Section */
    .character-groups {
        margin-bottom: 32px;
    }

    .character-group-card {
        background: var(--bg-card);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid var(--border);
        border-radius: 16px;
        padding: 1.25rem;
        margin-bottom: 16px;
        cursor: pointer;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    .character-group-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 3px;
        background: var(--primary-gradient);
        opacity: 0.8;
    }

    .character-group-card:hover {
        transform: translateY(-4px);
        border-color: var(--border-hover);
        box-shadow: 0 12px 32px rgba(130, 64, 255, 0.2);
    }

    .character-group-header {
        display: flex;
        align-items: center;
        gap: 16px;
    }

    .character-thumbnail {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid rgba(130, 64, 255, 0.4);
    }

    .character-info h3 {
        font-size: 1.1rem;
        font-weight: 600;
        margin: 0 0 4px 0;
        color: var(--text-primary);
    }

    .character-info p {
        color: var(--text-secondary);
        margin: 0;
        font-size: 0.9rem;
    }

    /* Content Grid */
    .content-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 16px;
        margin-top: 24px;
    }

    @media (min-width: 768px) {
        .content-grid {
            grid-template-columns: repeat(3, 1fr);
        }
    }

    @media (min-width: 1024px) {
        .content-grid {
            grid-template-columns: repeat(4, 1fr);
        }
    }

    @media (min-width: 1400px) {
        .content-grid {
            grid-template-columns: repeat(5, 1fr);
        }
    }

    .content-item {
        position: relative;
        aspect-ratio: 1;
        border-radius: 16px;
        overflow: hidden;
        cursor: pointer;
        background: var(--bg-card);
        border: 1px solid var(--border);
        transition: all 0.3s ease;
    }

    .content-item:hover {
        transform: scale(1.03);
        border-color: var(--border-hover);
        box-shadow: 0 12px 32px rgba(130, 64, 255, 0.2);
    }

    .content-item img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .content-item .video-overlay {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: rgba(0, 0, 0, 0.6);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border-radius: 50%;
        width: 56px;
        height: 56px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 1.5rem;
        transition: all 0.3s ease;
    }

    .content-item:hover .video-overlay {
        background: var(--primary-gradient);
        transform: translate(-50%, -50%) scale(1.1);
    }

    .content-badge {
        position: absolute;
        top: 10px;
        right: 10px;
        display: inline-flex;
        align-items: center;
        gap: 4px;
        padding: 4px 10px;
        border-radius: 12px;
        font-size: 0.7rem;
        font-weight: 600;
        text-transform: uppercase;
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
    }

    .badge-recent {
        background: rgba(16, 185, 129, 0.2);
        color: var(--success-light);
    }

    .badge-image {
        background: rgba(130, 64, 255, 0.2);
        color: var(--primary-light);
    }

    .badge-video {
        background: rgba(59, 130, 246, 0.2);
        color: var(--info-light);
    }

    /* Loading States */
    @keyframes shimmer {
        0% { background-position: 200% 0; }
        100% { background-position: -200% 0; }
    }

    .loading-spinner {
        text-align: center;
        padding: 60px 20px;
        color: var(--text-secondary);
    }

    .loading-spinner i {
        font-size: 2.5rem;
        color: var(--primary-light);
        margin-bottom: 16px;
        animation: spin 1.5s linear infinite;
    }

    @keyframes spin {
        to { transform: rotate(360deg); }
    }

    .loading-skeleton {
        background: linear-gradient(
            90deg,
            rgba(255, 255, 255, 0.04) 0%,
            rgba(255, 255, 255, 0.08) 50%,
            rgba(255, 255, 255, 0.04) 100%
        );
        background-size: 200% 100%;
        animation: shimmer 1.5s infinite;
        border-radius: 16px;
    }

    /* Empty State */
    .empty-state {
        text-align: center;
        padding: 80px 20px;
        background: var(--bg-card);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid var(--border);
        border-radius: 20px;
    }

    .empty-state-icon {
        font-size: 4rem;
        color: var(--text-muted);
        margin-bottom: 20px;
    }

    .empty-state h3 {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-primary);
        margin-bottom: 8px;
    }

    .empty-state p {
        color: var(--text-secondary);
        font-size: 0.95rem;
        margin-bottom: 24px;
    }

    .empty-state-btn {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: var(--primary-gradient);
        border: none;
        color: #fff;
        font-weight: 500;
        padding: 0.75rem 1.5rem;
        border-radius: 12px;
        transition: all 0.2s ease;
        box-shadow: 0 4px 12px rgba(130, 64, 255, 0.3);
        text-decoration: none;
    }

    .empty-state-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(130, 64, 255, 0.4);
        color: #fff;
    }

    /* Load More */
    #loadMoreContainer {
        text-align: center;
        margin-top: 32px;
        padding: 20px;
    }

    /* Modal Styles */
    .modal-content {
        background: rgba(15, 15, 26, 0.98);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border: 1px solid var(--border);
        border-radius: 20px;
        box-shadow: 0 24px 48px rgba(0, 0, 0, 0.5);
        max-height: 90vh;
    }

    .modal-header {
        background: transparent;
        border-bottom: 1px solid var(--border);
        padding: 1.25rem 1.5rem;
        position: sticky;
        top: 0;
        z-index: 10;
        border-radius: 20px 20px 0 0;
    }

    .modal-title {
        font-weight: 600;
        color: var(--text-primary);
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .modal-title i {
        color: var(--primary-light);
    }

    .modal-body {
        color: var(--text-primary);
        overflow-y: auto;
        max-height: calc(90vh - 60px);
        padding: 1.5rem;
    }

    .modal-body::-webkit-scrollbar {
        width: 6px;
    }

    .modal-body::-webkit-scrollbar-track {
        background: rgba(0, 0, 0, 0.2);
        border-radius: 3px;
    }

    .modal-body::-webkit-scrollbar-thumb {
        background: rgba(130, 64, 255, 0.3);
        border-radius: 3px;
    }

    .modal-body::-webkit-scrollbar-thumb:hover {
        background: rgba(130, 64, 255, 0.5);
    }

    .detail-item {
        margin-bottom: 12px;
        padding: 1rem;
        background: var(--bg-card);
        border: 1px solid var(--border);
        border-radius: 12px;
    }

    .detail-label {
        color: var(--text-muted);
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.03em;
        margin-bottom: 6px;
    }

    .detail-value {
        color: var(--text-primary);
        font-size: 1rem;
    }

    .navigation-buttons {
        display: flex;
        justify-content: space-between;
        gap: 12px;
        margin-top: 20px;
    }

    .nav-btn {
        flex: 1;
        background: var(--bg-input);
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: var(--text-secondary);
        padding: 0.625rem 1.25rem;
        border-radius: 12px;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
    }

    .nav-btn:hover:not(:disabled) {
        background: rgba(255, 255, 255, 0.1);
        border-color: var(--primary);
        color: var(--primary-light);
    }

    .nav-btn:disabled {
        opacity: 0.3;
        cursor: not-allowed;
    }

    /* Animations */
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .fade-in-up {
        animation: fadeInUp 0.3s ease;
    }

    /* Responsive adjustments */
    @media (max-width: 576px) {
        .history-container {
            padding: 70px 12px 100px;
        }

        .history-title {
            font-size: 1.5rem;
        }

        .filter-btn {
            padding: 0.5rem 1rem;
            font-size: 0.85rem;
        }

        .character-filter-item {
            min-width: 85px;
            max-width: 85px;
            padding: 10px;
        }

        .character-filter-avatar {
            width: 52px;
            height: 52px;
        }
    }
</style>

<body>
    {{> dashboard-nav}}

    <div class="history-container">
        <!-- Header -->
        <div class="history-header">
            <h1 class="history-title">
                <i class="bi bi-clock-history"></i> {{translations.history_page.title}}
            </h1>
            <p class="history-subtitle">{{translations.history_page.subtitle}}</p>
        </div>

        <!-- Content Type Filters -->
        <div class="filter-card">
            <div class="filter-section">
                <button class="filter-btn active" data-filter="all">
                    <i class="bi bi-grid-3x3"></i> {{translations.history_page.all_content}}
                </button>
                <button class="filter-btn" data-filter="recent">
                    <i class="bi bi-stars"></i> {{translations.history_page.recent}}
                </button>
                <button class="filter-btn" data-filter="images">
                    <i class="bi bi-image"></i> {{translations.history_page.images_only}}
                </button>
                <button class="filter-btn" data-filter="videos">
                    <i class="bi bi-camera-video"></i> {{translations.history_page.videos_only}}
                </button>
            </div>
        </div>

        <!-- Character Filter Carousel -->
        <div class="character-filter-section">
            <div class="character-filter-header">
                <span class="character-filter-title"><i class="bi bi-people-fill"></i> {{translations.history_page.filter_by_character}}</span>
                <div class="character-nav-buttons">
                    <button class="character-nav-btn" id="charScrollLeft" disabled>
                        <i class="bi bi-chevron-left"></i>
                    </button>
                    <button class="character-nav-btn" id="charScrollRight">
                        <i class="bi bi-chevron-right"></i>
                    </button>
                </div>
            </div>
            <div class="character-carousel-wrapper">
                <div class="character-carousel" id="characterCarousel">
                    <!-- All Characters Option -->
                    <div class="character-filter-item active" data-character="">
                        <div class="character-filter-avatar">
                            <div class="all-icon">
                                <i class="bi bi-grid-3x3-gap-fill"></i>
                            </div>
                        </div>
                        <span class="character-filter-name">{{translations.history_page.all_characters}}</span>
                    </div>
                    <!-- Character items will be populated dynamically -->
                </div>
            </div>
        </div>

        <!-- Character Groups (shown when grouping by character) -->
        <div id="characterGroups" class="character-groups" style="display: none;">
            <!-- Populated dynamically -->
        </div>

        <!-- Content Grid -->
        <div id="contentGrid" class="content-grid">
            <div class="loading-spinner" style="grid-column: 1 / -1;">
                <i class="bi bi-arrow-repeat"></i>
                <p>{{translations.history_page.loading_content}}</p>
            </div>
        </div>

        <!-- Load More / Infinite Scroll Trigger -->
        <div id="loadMoreContainer" style="display: none;">
            <div id="loadMoreSpinner" class="loading-spinner" style="display: none; padding: 20px;">
                <i class="bi bi-arrow-repeat"></i>
                <p>{{translations.history_page.loading_more}}</p>
            </div>
            <button id="loadMoreBtn" class="filter-btn">
                <i class="bi bi-arrow-down-circle"></i> {{translations.history_page.load_more}}
            </button>
        </div>

        <!-- Empty State -->
        <div id="emptyState" class="empty-state" style="display: none;">
            <div class="empty-state-icon"><i class="bi bi-images"></i></div>
            <h3>{{translations.history_page.empty_title}}</h3>
            <p>{{translations.history_page.empty_description}}</p>
            <a href="/dashboard/generation" class="empty-state-btn">
                <i class="bi bi-plus-circle"></i>
                {{translations.history_page.empty_cta}}
            </a>
        </div>
    </div>

    <!-- Content Detail Modal -->
    <div class="modal fade" id="contentModal" tabindex="-1" aria-labelledby="contentModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="contentModalLabel">
                        <i class="bi bi-info-circle"></i>
                        {{translations.history_page.modal_title}}
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div id="modalContentArea">
                        <!-- Content will be loaded here -->
                    </div>
                </div>
            </div>
        </div>
    </div>

    {{> dashboard-footer}}

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/moment@2.29.1/moment.min.js"></script>
    <script src="/js/history-gallery.js"></script>
</body>
</html>
